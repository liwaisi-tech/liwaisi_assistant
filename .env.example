# Required -- Platform secrets
OPENROUTER_API_KEY=your-api-key-here
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Optional -- Override defaults
# POSTGRES_PASSWORD=liwaisi_dev
# CORS_ORIGINS=*
# LOG_LEVEL=info
# Note: per spec-architecture-model-selection-centralization.md, DEFAULT_MODEL
# and MODEL_* env variables are no longer read. Every user picks their model
# in onboarding; the product default is baked into the backend as
# PRODUCT_DEFAULT_MODEL (currently google/gemma-4-31b-it).
