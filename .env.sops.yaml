# SOPS-encrypted environment file for Liwaisi Assistant.
# To use: run 'make sops-setup' first, then 'make sops-edit' to add real values.
#
# These values will be encrypted by SOPS. Only the values are encrypted;
# keys remain in plaintext for readable git diffs.
OPENROUTER_API_KEY: "REPLACE_WITH_YOUR_API_KEY"
GOOGLE_CLIENT_ID: "REPLACE_WITH_YOUR_GOOGLE_CLIENT_ID"
VITE_GOOGLE_CLIENT_ID: "REPLACE_WITH_YOUR_GOOGLE_CLIENT_ID"
POSTGRES_PASSWORD: "liwaisi_dev"
